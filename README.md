# Cloud Fun Facts Generator

Static frontend (HTML/CSS/JS) for an AWS serverless app (API Gateway -> Lambda -> DynamoDB).

## Run locally
Open this folder in VS Code and start Live Server on `index.html`. No build step or dependencies.

## Files
- index.html
- style.css
- script.js
